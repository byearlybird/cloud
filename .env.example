# JWT Secrets
# IMPORTANT: These secrets must be at least 32 characters long for security
# Generate secure secrets using: openssl rand -base64 32
# Or use: bun run -e 'console.log(require("crypto").randomBytes(32).toString("base64"))'

# Access token secret - used for short-lived tokens (15-60 minutes)
ACCESS_TOKEN_SECRET=CHANGE_ME_use_openssl_rand_base64_32_to_generate_strong_secret

# Refresh token secret - used for long-lived tokens (7-30 days)
REFRESH_TOKEN_SECRET=CHANGE_ME_use_openssl_rand_base64_32_to_generate_strong_secret

# Token Expiry (optional)
# Access token expiry in seconds (default: 900 = 15 minutes)
# ACCESS_TOKEN_EXPIRY=900

# Refresh token expiry in seconds (default: 604800 = 7 days)
# REFRESH_TOKEN_EXPIRY=604800

# Database
# Path to SQLite database file
DATABASE_PATH=./data/database.sqlite

# CORS
# Comma-separated list of allowed origins for CORS (default: http://localhost:3000)
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
