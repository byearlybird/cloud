meta {
  name: refresh
  type: http
  seq: 2
}

post {
  url: http://localhost:3000/api/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": "someToken"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
