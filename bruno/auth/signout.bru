meta {
  name: signout
  type: http
  seq: 2
}

post {
  url: http://localhost:3000/api/auth/signout
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": "someToken"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
