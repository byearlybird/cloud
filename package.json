{
  "name": "@byearlybird/cloud",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    "./types": {
      "types": "./dist/types.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "format": "oxfmt",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix"
  },
  "dependencies": {
    "@clerk/backend": "^2.29.2",
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "oxfmt": "^0.23.0",
    "oxlint": "^1.38.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.4.0"
  }
}
